#!/bin/zsh
set -euo pipefail

SCRIPT_DIR="${0:A:h}"
PROJECT_PATH="$SCRIPT_DIR/Mercury/Mercury.xcodeproj"
SCHEME="Mercury"
CONFIGURATION="Debug"

xcodebuild \
  -project "$PROJECT_PATH" \
  -scheme "$SCHEME" \
  -configuration "$CONFIGURATION" \
  -destination "platform=macOS" \
  build
